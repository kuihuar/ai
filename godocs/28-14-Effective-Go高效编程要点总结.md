# Effective Go 高效编程要点总结

## 一、概述

[Effective Go](https://go.dev/doc/effective_go) 是 Go 官方文档，提供了编写清晰、高效、符合 Go 语言习惯的代码的建议。本文档总结了其中的关键要点。

> **注意**: Effective Go 文档写于 2009 年，虽然语言本身很稳定，但关于库、构建系统、测试、模块等方面的内容较少。不过，它仍然是理解 Go 语言本身的重要指南。

## 二、格式化（Formatting）

### 2.1 使用 gofmt

**核心原则**: 让机器处理格式化问题，而不是人工争论。

```go
// ✅ 使用 gofmt 自动格式化
// 运行: gofmt -w .
// 或: go fmt ./...

// gofmt 会自动处理：
// - 缩进和对齐
// - 注释位置
// - 代码风格
```

**关键点**:
- ✅ **使用 `gofmt` 或 `go fmt` 格式化代码**
- ✅ **不要手动调整格式，让工具处理**
- ✅ **所有标准库代码都使用 gofmt 格式化**

### 2.2 格式化规则

#### 缩进
- 使用 **Tab** 进行缩进（gofmt 默认）
- 只有在必须时才使用空格

#### 行长度
- Go **没有行长度限制**
- 如果一行太长，换行并额外缩进一个 Tab

#### 括号
- Go 需要的括号比 C 和 Java 少
- 控制结构（`if`、`for`、`switch`）不需要括号
- 运算符优先级更清晰

```go
// ✅ Go 的运算符优先级更清晰
x<<8 + y<<16  // 含义明确，不需要括号
```

## 三、注释（Commentary）

### 3.1 注释类型

```go
// ✅ 行注释（推荐）
// 这是行注释

/* ✅ 块注释（用于包注释或大段代码） */
/*
 * 这是块注释
 * 通常用于包级别的文档
 */
```

### 3.2 文档注释（Doc Comments）

**关键规则**: 出现在顶级声明之前且没有空行的注释，被视为文档注释。

```go
// Package math provides basic constants and mathematical functions.
package math

// Pi is the ratio of a circle's circumference to its diameter.
const Pi = 3.14159265358979323846264338327950288419716939937510582097494459

// Sin returns the sine of the angle x (in radians).
func Sin(x float64) float64
```

**特点**:
- 文档注释是 Go 包的主要文档来源
- 使用 `go doc` 可以查看文档注释
- 应该描述"做什么"，而不是"怎么做"

## 四、命名（Names）

### 4.1 可见性规则

**核心规则**: 首字母大小写决定可见性
- **大写开头**: 导出（public）
- **小写开头**: 未导出（private）

```go
// ✅ 导出的（public）
type User struct {
    Name string  // 导出字段
}

func GetUser() *User {  // 导出函数
    return &User{}
}

// ✅ 未导出的（private）
type user struct {
    name string  // 未导出字段
}

func getUser() *user {  // 未导出函数
    return &user{}
}
```

### 4.2 包命名

**原则**:
- ✅ **简短、简洁、有表现力**
- ✅ **小写、单词、无下划线、无混合大小写**
- ✅ **包名是导入目录的基名**

```go
// ✅ 好的包名
import "bytes"      // 包名: bytes
import "encoding/base64"  // 包名: base64（不是 encoding_base64）

// ❌ 不好的包名
import "my_package"  // 避免下划线
import "MyPackage"   // 避免混合大小写
```

**关键点**:
- 包名应该简短，因为使用包的人会频繁输入
- 包名不需要全局唯一，导入路径决定实际使用的包
- 导出的名称可以利用包名避免重复（如 `bufio.Reader` 而不是 `bufio.BufReader`）

### 4.3 接口命名

**原则**:
- 单方法接口：方法名 + "er" 后缀
- 多方法接口：描述性的名称

```go
// ✅ 单方法接口
type Reader interface {
    Read([]byte) (int, error)
}

type Writer interface {
    Write([]byte) (int, error)
}

// ✅ 多方法接口
type ReadWriter interface {
    Reader
    Writer
}
```

### 4.4 变量和函数命名

**原则**:
- ✅ **使用驼峰命名（MixedCaps）**
- ✅ **简短但有意义**
- ✅ **避免缩写，除非是广泛使用的**

```go
// ✅ 好的命名
func GetUserByID(id int) *User
var maxRetries = 3
const defaultTimeout = 5 * time.Second

// ❌ 不好的命名
func GetUsrById(i int) *User  // 过度缩写
var max_retries = 3           // 使用下划线
const DEFAULT_TIMEOUT = 5 * time.Second  // 全大写常量（Go 风格）
```

## 五、控制结构

### 5.1 if 语句

```go
// ✅ Go 的 if 不需要括号
if x > 0 {
    return y
}

// ✅ if 可以包含初始化语句
if err := file.Close(); err != nil {
    return err
}
```

### 5.2 for 循环

```go
// ✅ 三种形式的 for
// 1. 类似 C 的 for
for init; condition; post { }

// 2. 类似 while
for condition { }

// 3. 无限循环
for { }

// ✅ range 遍历
for key, value := range map {
    // ...
}

for index, value := range slice {
    // ...
}
```

### 5.3 switch 语句

```go
// ✅ switch 不需要 break
switch tag {
case 0, 1, 2, 3:
    s1()
case 4, 5, 6, 7:
    s2()
default:
    s3()
}

// ✅ switch 可以没有条件（类似 if-else 链）
switch {
case x < 0:
    return -x
case x == 0:
    return 0
default:
    return x
}
```

## 六、函数

### 6.1 多返回值

```go
// ✅ Go 函数可以返回多个值
func nextInt(b []byte, i int) (int, int) {
    for i < len(b) && !isDigit(b[i]) {
        i++
    }
    x := 0
    for i < len(b) && isDigit(b[i]) {
        x = x*10 + int(b[i]) - '0'
        i++
    }
    return x, i
}
```

### 6.2 命名返回值

```go
// ✅ 命名返回值
func ReadFull(r Reader, buf []byte) (n int, err error) {
    for len(buf) > 0 && err == nil {
        var nr int
        nr, err = r.Read(buf)
        n += nr
        buf = buf[nr:]
    }
    return
}
```

**注意**: 命名返回值可以增加可读性，但不要过度使用。

### 6.3 defer 语句

```go
// ✅ defer 用于清理资源
func Contents(filename string) (string, error) {
    f, err := os.Open(filename)
    if err != nil {
        return "", err
    }
    defer f.Close()  // 确保文件被关闭
    
    var result []byte
    buf := make([]byte, 100)
    for {
        n, err := f.Read(buf[0:])
        result = append(result, buf[0:n]...)
        if err != nil {
            if err == io.EOF {
                break
            }
            return "", err
        }
    }
    return string(result), nil
}
```

**关键点**:
- `defer` 语句在函数返回前执行
- 多个 `defer` 按 LIFO（后进先出）顺序执行
- `defer` 可以修改命名返回值

## 七、数据（Data）

### 7.1 new 和 make

**区别**:
- `new(T)`: 分配零值内存，返回 `*T`
- `make(T)`: 创建 slice、map、channel，返回 `T`

```go
// ✅ new 用于值类型
p := new(int)    // *int，值为 0
p := new(User)   // *User，字段为零值

// ✅ make 用于引用类型
s := make([]int, 10)      // []int，长度为 10
m := make(map[string]int) // map[string]int
c := make(chan int)       // chan int
```

### 7.2 数组和切片

```go
// ✅ 数组（值类型）
var a [10]int

// ✅ 切片（引用类型）
var s []int
s = make([]int, 10)      // 长度为 10
s = make([]int, 10, 20)  // 长度为 10，容量为 20

// ✅ 切片操作
s = s[1:4]  // 切片
s = append(s, 5)  // 追加
```

### 7.3 Map

```go
// ✅ Map 的使用
m := make(map[string]int)
m["key"] = 1
value, ok := m["key"]  // 检查键是否存在
if ok {
    // 键存在
}
```

### 7.4 结构体

```go
// ✅ 结构体定义
type Point struct {
    X, Y int
}

// ✅ 结构体字面量
p := Point{1, 2}
p := Point{X: 1, Y: 2}

// ✅ 结构体方法
func (p *Point) Distance() float64 {
    return math.Sqrt(float64(p.X*p.X + p.Y*p.Y))
}
```

## 八、初始化（Initialization）

### 8.1 常量

```go
// ✅ 常量定义
const (
    Sunday = iota
    Monday
    Tuesday
    Wednesday
    Thursday
    Friday
    Saturday
)
```

### 8.2 init 函数

```go
// ✅ init 函数在包导入时自动执行
func init() {
    // 初始化代码
}

// ✅ 可以有多个 init 函数
func init() {
    // 第一个 init
}

func init() {
    // 第二个 init
}
```

**使用场景**:
- 包级别的初始化
- 验证环境变量
- 注册资源

**注意**: 如果库无法正确初始化，可以 panic。

## 九、错误处理

### 9.1 错误返回

```go
// ✅ 错误作为最后一个返回值
func ReadFile(filename string) ([]byte, error) {
    f, err := os.Open(filename)
    if err != nil {
        return nil, err
    }
    defer f.Close()
    // ...
    return data, nil
}
```

### 9.2 错误检查

```go
// ✅ 立即检查错误
if err != nil {
    return err
}

// ✅ 错误包装
if err != nil {
    return fmt.Errorf("read file: %w", err)
}
```

## 十、Panic 和 Recover

### 10.1 Panic

**使用场景**:
- 不可恢复的错误
- 程序无法继续执行的情况

```go
// ✅ 合理的 panic 使用
var user = os.Getenv("USER")

func init() {
    if user == "" {
        panic("no value for $USER")
    }
}
```

### 10.2 Recover

**关键点**:
- `recover` 只能在 `defer` 函数中使用
- `recover` 停止 panic 的传播
- 返回传递给 `panic` 的值

```go
// ✅ recover 的使用
func safelyDo(work *Work) {
    defer func() {
        if err := recover(); err != nil {
            log.Println("work failed:", err)
        }
    }()
    do(work)
}
```

**最佳实践**:
- ✅ **在包内部使用 panic/recover 处理错误**
- ✅ **不要向包外部暴露 panic**
- ✅ **将内部 panic 转换为 error 返回**

## 十一、Web 服务器示例

Effective Go 文档提供了一个完整的 Web 服务器示例，展示了 Go 的简洁性和强大功能：

```go
package main

import (
    "flag"
    "html/template"
    "log"
    "net/http"
)

var addr = flag.String("addr", ":1718", "http service address")
var templ = template.Must(template.New("qr").Parse(templateStr))

func main() {
    flag.Parse()
    http.Handle("/", http.HandlerFunc(QR))
    err := http.ListenAndServe(*addr, nil)
    if err != nil {
        log.Fatal("ListenAndServe:", err)
    }
}

func QR(w http.ResponseWriter, req *http.Request) {
    templ.Execute(w, req.FormValue("s"))
}
```

**关键点**:
- 简洁的代码实现完整功能
- 使用标准库的 `html/template` 包
- 展示了 Go 的简洁性和表达能力

## 十二、Effective Go 的核心原则总结

### 12.1 代码风格

| 原则 | 说明 |
|------|------|
| **使用 gofmt** | 让工具处理格式化 |
| **简洁命名** | 简短但有意义 |
| **可见性规则** | 首字母大小写决定可见性 |
| **文档注释** | 顶级声明前的注释是文档 |

### 12.2 编程实践

| 实践 | 说明 |
|------|------|
| **多返回值** | 利用 Go 的多返回值特性 |
| **defer 清理** | 使用 defer 确保资源释放 |
| **错误处理** | 错误作为最后一个返回值 |
| **panic/recover** | 在包内部使用，不向外暴露 |

### 12.3 数据结构

| 类型 | 使用场景 |
|------|---------|
| **new** | 值类型（返回指针） |
| **make** | 引用类型（slice、map、channel） |
| **数组** | 固定大小 |
| **切片** | 动态大小 |
| **Map** | 键值对 |
| **结构体** | 组合数据 |

## 十三、与之前学习内容的关联

### 13.1 错误处理

Effective Go 强调：
- 错误作为最后一个返回值
- 立即检查错误
- 在包内部使用 panic/recover

这与我们之前学习的错误处理最佳实践一致。

### 13.2 Panic 和 Recover

Effective Go 明确指出：
- `recover` 只能在 `defer` 函数中使用
- 应该在包内部使用 panic/recover
- 不要向包外部暴露 panic

这与我们之前学习的 Recovery 机制完全一致。

### 13.3 并发

虽然 Effective Go 文档较老，但其中的并发模式（goroutine、channel）仍然是现代 Go 编程的基础。

## 十四、总结

### 14.1 关键要点

1. ✅ **使用 gofmt 格式化代码** - 让工具处理，不要争论
2. ✅ **遵循命名约定** - 首字母大小写决定可见性
3. ✅ **使用 defer 清理资源** - 确保资源正确释放
4. ✅ **错误作为返回值** - 不要使用异常
5. ✅ **在包内部使用 panic/recover** - 不向外暴露

### 14.2 学习建议

- 📖 **阅读完整的 Effective Go 文档**: [https://go.dev/doc/effective_go](https://go.dev/doc/effective_go)
- 📖 **阅读标准库代码**: 标准库是学习 Go 的最佳示例
- 📖 **实践这些原则**: 在实际项目中应用这些原则

### 14.3 注意事项

Effective Go 文档写于 2009 年，虽然语言本身很稳定，但：
- ⚠️ **关于库的内容较少**
- ⚠️ **没有涉及模块系统**
- ⚠️ **没有涉及现代测试实践**
- ⚠️ **没有涉及构建系统**

但它仍然是理解 Go 语言本身的重要指南！

记住这些原则，可以编写更符合 Go 语言习惯、更清晰、更高效的代码！

